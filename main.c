#include "include/menu_manager.h"  // Include the header file where welcomeMenu is declared
#include "stdlib.h"

int main(void) {
    system("clear");
    welcomeMenu();  // Call the function to display the welcome menu and start the program
    return 0;       // Return 0 to indicate successful execution
}
